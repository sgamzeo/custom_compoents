/// logging severity levels
enum LogLevel { debug, info, warning, error }

// state management status
enum StateStatus { initial, loading, success, error }

// error categories
enum ErrorType { network, validation, auth, database, unknown }
